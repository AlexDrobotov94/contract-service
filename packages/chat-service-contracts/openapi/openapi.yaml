# generated by kvint-openapi-full-agent
# source: .agent-workspace/transport-scan.2026-04-13T10-00-00Z.json
# framework: NestJS
# generated: 2026-04-13T10:00:00Z
# controllers: 2
# endpoints: 9

openapi: "3.1.0"

info:
  title: Chat Service
  version: 1.0.0
  description: ""

servers:
  - url: http://localhost:3000
    description: Local development

tags:
  - name: Health
    description: ""
  - name: Chats
    description: ""

security:
  - bearerAuth: []

paths:
  /health/live:
    get:
      operationId: HealthController_live
      summary: ""
      tags:
        - Health
      parameters: []
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthLiveResponse"
      security: []

  /health/ready:
    get:
      operationId: HealthController_ready
      summary: ""
      tags:
        - Health
      parameters: []
      responses:
        "200":
          description: ""
      security: []

  /chats:
    get:
      operationId: ChatsController_listChats
      summary: ""
      tags:
        - Chats
      parameters:
        - name: project
          in: query
          required: true
          schema:
            type: string
            minLength: 1
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum:
              - createdAt
              - updatedAt
            default: createdAt
        - name: sortOrder
          in: query
          required: false
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ChatStatus"
        - name: id
          in: query
          required: false
          schema:
            type: string
            pattern: "^[0-9a-fA-F]{24}$"
        - name: script
          in: query
          required: false
          schema:
            type: string
        - name: nlu
          in: query
          required: false
          schema:
            type: string
        - name: createdBy
          in: query
          required: false
          schema:
            type: string
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatListResponse"
      security:
        - bearerAuth: []

    post:
      operationId: ChatsController_create
      summary: ""
      tags:
        - Chats
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatInput"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Chat"
      security:
        - bearerAuth: []

  /chats/{chatId}:
    get:
      operationId: ChatsController_getChat
      summary: ""
      tags:
        - Chats
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Chat"
      security:
        - bearerAuth: []

  /chats/{chatId}/start:
    post:
      operationId: ChatsController_startSession
      summary: ""
      tags:
        - Chats
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: string
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartSessionOutput"
      security:
        - bearerAuth: []

  /chats/{chatId}/close:
    post:
      operationId: ChatsController_closeSession
      summary: ""
      tags:
        - Chats
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: string
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloseSessionOutput"
      security:
        - bearerAuth: []

  /chats/{chatId}/build-info:
    get:
      operationId: ChatsController_getBuildInfo
      summary: ""
      tags:
        - Chats
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetBuildInfoResponse"
      security:
        - bearerAuth: []

  /chats/{chatId}/timeline:
    get:
      operationId: ChatsController_getTimeline
      summary: ""
      tags:
        - Chats
      parameters:
        - name: chatId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetTimelineResponse"
      security:
        - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    HealthLiveResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string

    ChatStatus:
      type: string
      enum:
        - active
        - pending
        - completed
        - failed

    ChatUser:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1

    Chat:
      type: object
      required:
        - id
        - status
        - stateVersion
        - buildId
        - project
        - user
        - sessionIds
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        status:
          $ref: "#/components/schemas/ChatStatus"
        stateVersion:
          type: number
          default: 0
        buildId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        project:
          type: string
          minLength: 1
        user:
          $ref: "#/components/schemas/ChatUser"
        activeSessionId:
          type: string
          format: uuid
        activeInstanceQueue:
          type: string
          minLength: 1
        sessionIds:
          type: array
          items:
            type: string
            format: uuid
          default: []
        errorMessage:
          type: string
          minLength: 1
        createdAt:
          type: string
          minLength: 1
        updatedAt:
          type: string
          minLength: 1

    CreateChatInput:
      type: object
      required:
        - buildId
        - project
      properties:
        buildId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        project:
          type: string
          minLength: 1

    ChatListItemScript:
      type: object
      required:
        - id
        - name
        - version
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        name:
          type: string
        version:
          type: number

    ChatListItemNlu:
      type: object
      required:
        - name
        - version
      properties:
        name:
          type: string
        version:
          type: number

    ChatListItem:
      type: object
      required:
        - id
        - status
        - script
        - nlu
        - createdBy
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        status:
          $ref: "#/components/schemas/ChatStatus"
        script:
          oneOf:
            - $ref: "#/components/schemas/ChatListItemScript"
            - type: "null"
        nlu:
          type: array
          items:
            $ref: "#/components/schemas/ChatListItemNlu"
        createdBy:
          type: string
        createdAt:
          type: string
          minLength: 1
        updatedAt:
          type: string
          minLength: 1

    ChatListResponse:
      type: object
      required:
        - items
        - total
        - offset
        - limit
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ChatListItem"
        total:
          type: integer
          minimum: 0
        offset:
          type: integer
          minimum: 0
        limit:
          type: integer
          minimum: 1

    StartSessionOutput:
      type: object
      required:
        - ok
        - chatId
        - sessionId
      properties:
        ok:
          type: boolean
          enum:
            - true
        chatId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        sessionId:
          type: string
          format: uuid

    CloseSessionOutput:
      type: object
      required:
        - ok
        - chatId
        - closedSessionId
      properties:
        ok:
          type: boolean
          enum:
            - true
        chatId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        closedSessionId:
          type: string
          format: uuid

    BuildInfoScript:
      type: object
      required:
        - name
        - version
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        name:
          type: string
        version:
          type: string

    BuildInfoNluItem:
      type: object
      required:
        - name
        - version
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        name:
          type: string
        version:
          type: string

    GetBuildInfoResponse:
      type: object
      required:
        - id
        - script
        - nlu
      properties:
        id:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        script:
          $ref: "#/components/schemas/BuildInfoScript"
        nlu:
          type: array
          items:
            $ref: "#/components/schemas/BuildInfoNluItem"

    TimelineEventType:
      type: string
      enum:
        - session.started
        - session.ended

    TimelineEventItem:
      type: object
      required:
        - kind
        - key
        - chatId
        - sessionId
        - type
        - occurredAt
      properties:
        kind:
          type: string
          enum:
            - event
        key:
          type: string
          minLength: 1
        chatId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        sessionId:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/TimelineEventType"
        occurredAt:
          type: number

    TimelineMessageItem:
      type: object
      required:
        - kind
        - key
        - chatId
        - sessionId
        - correlationId
        - role
        - sideIndex
        - turnIndex
        - text
        - occurredAt
      properties:
        kind:
          type: string
          enum:
            - message
        key:
          type: string
          minLength: 1
        chatId:
          type: string
          pattern: "^[0-9a-fA-F]{24}$"
        sessionId:
          type: string
          format: uuid
        correlationId:
          type: string
          format: uuid
        role:
          type: string
          enum:
            - user
            - bot
        sideIndex:
          type: integer
          enum:
            - 0
            - 1
        turnIndex:
          type: integer
          minimum: 0
        text:
          type: string
        occurredAt:
          oneOf:
            - type: number
            - type: "null"

    TimelineItem:
      oneOf:
        - $ref: "#/components/schemas/TimelineEventItem"
        - $ref: "#/components/schemas/TimelineMessageItem"
      discriminator:
        propertyName: kind

    GetTimelineResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TimelineItem"
