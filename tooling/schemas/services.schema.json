{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "services.schema.json",
  "definitions": {
    "serviceId": {
      "type": "string",
      "enum": ["chat-service", "dialer-service"]
    },
    "serviceRef": {
      "type": "string",
      "enum": ["chat-service", "dialer-service"]
    }
  }
}
